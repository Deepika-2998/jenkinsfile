pipeline {
agent any
  stages {
    stage('TEST') {
      steps {
        echo "hello"
      }
    }
  }
  post {
    
    aborted {
      echo "This will run when build fails"
      error "Exiting with failure code due to manual abort."
    }
    
  }
}
