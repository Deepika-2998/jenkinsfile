pipeline {
agent any
  stages {
    stage('TEST') {
      steps {
        echo "hello"
      }
    }
  }
  post {
    
    failure {
      sh 'exit 1'
      echo "This will run when build fails"
    }
    
  }
}
