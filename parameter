pipeline {
  agent any
  parameters {
    booleanParam(name: 'run-project', description: 'Whether to run the project or not', defaultValue: true)
  }
  stages {
    stage ('stage1') {
      steps {
        echo "BOOLEAN: ${params.run-project}"
      }
    }
  }
}
