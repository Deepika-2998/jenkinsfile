pipeline {
    agent any
    environment {
        BRANCH = 'main'
        GIT_URL = 'https://github.com/harshaprakash100/ip_app.git'
        CRED_ID = 'github_hp'
    }

    stages {
        stage('checkout') {
            steps {
                  git branch: "${env.BRANCH}",
                    credentialsId: "${env.CRED_ID}",
                    url: "${env.GIT_URL}"
            }
        }
    }
}
