pipline{
  agent any
  enironmental{
      GIT_URL="https://github.com/Deepika-2998/jenkinsfile.git"
      GIT_BRANCH="main"
      GIT_CREDENTIALS=""
  stages{
    stage('build'){
        script{
            url: "${GIT_URL}"
            git branch: "${GIT_BRANCH}"
            credientalsid: "${}"
            }
      }
  }
